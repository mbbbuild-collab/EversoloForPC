<Window x:Class="Eversolo.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Eversolo"
        Title="EversoloForPC" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize" SizeToContent="WidthAndHeight"
        FontFamily="Segoe UI" Foreground="#F1EFE8" TextOptions.TextFormattingMode="Display"
        MouseLeftButtonDown="OnDrag" MouseDoubleClick="OnToggleMode">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="{local:Tr menu.big}" Click="OnBig"/>
            <MenuItem Header="{local:Tr menu.mode}" Click="OnToggleMode"/>
            <MenuItem x:Name="LocalMenu" Header="{local:Tr menu.local}" IsCheckable="True" Click="OnLocalToggle"/>
            <MenuItem Header="{local:Tr menu.settings}" Click="OnSettings"/>
            <Separator/>
            <MenuItem Header="{local:Tr menu.hide}" Click="OnHide"/>
            <MenuItem Header="{local:Tr menu.exit}" Click="OnExit"/>
        </ContextMenu>
    </Window.ContextMenu>
    <Border Background="#EB1C1C1E" CornerRadius="12" BorderBrush="#33FFFFFF" BorderThickness="0.5" Padding="12">
        <Grid>
            <StackPanel x:Name="Full" Width="300">
                <DockPanel>
                    <Border DockPanel.Dock="Right" x:Name="BadgeBox" Background="#22085041" CornerRadius="10" Padding="8,2">
                        <TextBlock x:Name="Badge" Text="—" FontSize="11" Foreground="#9FE1CB"/>
                    </Border>
                    <TextBlock x:Name="Device" Text="Eversolo" FontSize="12" Foreground="#B4B2A9" VerticalAlignment="Center"/>
                </DockPanel>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="84"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Width="84" Height="84">
                        <Rectangle RadiusX="8" RadiusY="8" Fill="#26215C"/>
                        <TextBlock x:Name="CoverFallback" Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets" FontSize="28"
                                   Foreground="#CECBF6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Rectangle RadiusX="8" RadiusY="8">
                            <Rectangle.Fill>
                                <ImageBrush x:Name="CoverBrush" Stretch="UniformToFill"/>
                            </Rectangle.Fill>
                        </Rectangle>
                    </Grid>
                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                        <TextBlock x:Name="TitleT" Text="—" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="ArtistT" FontSize="13" Foreground="#D3D1C7" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                        <TextBlock x:Name="AlbumT" FontSize="12" Foreground="#888780" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                        <TextBlock x:Name="FmtT" FontSize="11" Foreground="#B4B2A9" TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
                <Grid Height="4" Margin="0,12,0,0">
                    <Rectangle Fill="#3A3A3C" RadiusX="2" RadiusY="2"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="ProgCol" Width="0*"/>
                            <ColumnDefinition x:Name="RestCol" Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle Fill="#7F77DD" RadiusX="2" RadiusY="2"/>
                    </Grid>
                </Grid>
                <DockPanel Margin="0,4,0,0">
                    <TextBlock x:Name="DurT" DockPanel.Dock="Right" Text="0:00" FontSize="11" Foreground="#888780"/>
                    <TextBlock x:Name="PosT" Text="0:00" FontSize="11" Foreground="#888780"/>
                </DockPanel>
                <DockPanel Margin="0,6,0,0">
                    <TextBlock x:Name="VolT" DockPanel.Dock="Right" FontSize="12" Foreground="#B4B2A9" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource Glyph}" Content="&#xE892;" Click="OnPrev" ToolTip="{local:Tr tip.prev}"/>
                        <Button Style="{StaticResource Glyph}" x:Name="PlayBtn" Content="&#xE768;" Click="OnPlayPause" ToolTip="{local:Tr tip.playpause}"/>
                        <Button Style="{StaticResource Glyph}" Content="&#xE893;" Click="OnNext" ToolTip="{local:Tr tip.next}"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>

            <Grid x:Name="Mini" Width="270" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="36"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Width="36" Height="36">
                    <Rectangle RadiusX="6" RadiusY="6" Fill="#26215C"/>
                    <Rectangle RadiusX="6" RadiusY="6">
                        <Rectangle.Fill>
                            <ImageBrush x:Name="MiniCoverBrush" Stretch="UniformToFill"/>
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <TextBlock x:Name="MiniTitle" Text="—" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="MiniSub" FontSize="11" Foreground="#B4B2A9" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Button Grid.Column="3" Style="{StaticResource Glyph}" x:Name="MiniPlayBtn" Content="&#xE768;" Width="28" Height="28" Click="OnPlayPause"/>
            </Grid>
        </Grid>
    </Border>
</Window>
